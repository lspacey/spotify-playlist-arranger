#    python -m venv venv
#    venv\Scripts\activate
#    pip install -r requirements.txt
#    pip list

# Spotify Web API
spotipy>=2.23.0

# Capture audio – WASAPI loopback (Windows)
# If `pip install pyaudiowpatch` fails, you need Visual C++ Build Tools:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
pyaudiowpatch>=0.2.12

# Audio analysis
librosa>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0

# Terminal UI
rich>=13.0.0

# Environment-variable loader
python-dotenv>=1.0.0

# Web UI framework
nicegui>=1.4.0

# Audio metadata (for local file support)
mutagen>=1.47.0

# MERT neural embeddings (for audio vector analysis)
# ─── CPU-only (default) ────────────────────────────────────────────────────────
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0,<4.49
accelerate>=0.20.0

# ─── CUDA acceleration (Windows / Linux) ───────────────────────────────────────
#   Check your CUDA version:  nvidia-smi
#   Visit https://pytorch.org/get-started/locally/ for the correct index URL.
#
#   Example – CUDA 12.1:
#     pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
#     pip install transformers>=4.30.0,<4.49 accelerate>=0.20.0
#
#   Example – CUDA 11.8:
#     pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#     pip install transformers>=4.30.0,<4.49 accelerate>=0.20.0
#
#   Verify:  python -c "import torch; print(torch.cuda.is_available())"
#   If False →  pip uninstall torch torchvision
#               pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# LLM backends (pick at least one)
ollama>=0.1.0        # local LLM  (free, offline)
openai>=1.0.0        # for DeepSeek / Mistral APIs

# HTTP requests (for Spotify API direct calls)
requests>=2.28.0